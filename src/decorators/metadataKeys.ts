/**
 * reflect-metadata keys used by Atlex DI decorators.
 */
export const ATLEX_INJECTABLE = 'atlex:injectable'

/**
 * Marks a class resolved as a singleton instance.
 */
export const ATLEX_SINGLETON = 'atlex:singleton'

/**
 * Per-parameter explicit injection tokens (constructor index → token).
 */
export const ATLEX_INJECT_PARAMS = 'atlex:inject-params'
